{
  "name": "full-stack-takehome",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "cd client && npm run dev & cd server && npm run dev",
    "test": "cd client && jest & cd server && jest & npx prettier --check .",
    "lint": "npx prettier . --write",
    "docker-dev:hot": "docker-compose -f docker-compose-dev-hot.yml up",
    "docker-build-dev": "docker-compose -f docker-compose-dev-hot.yml up --build",
    "install-deps": "npm install & cd client && npm install & cd server && npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/preset-env": "^7.24.1",
    "@babel/preset-typescript": "^7.24.1",
    "@types/jest": "^29.5.12",
    "dotenv": "^16.4.5",
    "jest": "^29.7.0",
    "prettier": "^3.2.5",
    "ts-jest": "^29.1.2"
  }
}
